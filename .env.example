# Base URL of the AS Travel platform API this SPA talks to (bearer-token auth).
# NOTE: Vite inlines VITE_* vars at BUILD time, so set this before building for
# each environment (e.g. https://csrm.test locally, the prod API URL on deploy).
VITE_API_URL=https://csrm.test
